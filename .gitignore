node_modules
# dist — removed so Hostinger can deploy from GitHub
.env
.env.local
.env.*.local
.firebase
*.log
.DS_Store
.vscode
.idea
functions/node_modules
.openclaude/

# Local Netlify folder
.netlify

# Lint output dumps (one-off, never commit)
errors.json
eslint-errors.json
*-errors.json

# One-off migration scripts
fix-tailwind.js

# Secrets / API keys
rzp-key.csv
*.csv
serviceAccountKey*.json
